export class Station {
    _id: Record<string, any>
    name: string
    code: number
    state: string
    city: string
    latitude: number
    longitude: number
    zone: string
    docs: Record<string, any>
}
